import React from 'react';

const Divider = ({ style }) => {
    return (
        <div
            className="section-divider"
            style={{
                ...style
            }}
        />
    );
};

export default Divider;
